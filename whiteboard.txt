nook:
└──> irc
    └──> sends a signal to the webview page everytime something happens
    └──> logs everything into a file
└──> webview
    └──> open up the html file
    └──> process some signal everytime something happens


└──> eg.: new message -> add message to log with id#x -> tell webview there's a new message in the log with id#x